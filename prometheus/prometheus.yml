global:
  scrape_interval: 5s
  evaluation_interval: 5s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "sample-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:5000"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
